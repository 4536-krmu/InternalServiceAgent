version: '3.8'

services:
  veridian-it-portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: veridian_corp_it_portal
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - ENVIRONMENT=production
    restart: unless-stopped
